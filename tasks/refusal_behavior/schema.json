{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["should_refuse", "response"],
  "properties": {
    "should_refuse": { "type": "boolean" },
    "response": { "type": "string" },
    "safe_alternative": { "type": "string" }
  }
}

