app:
  name: pranik-benchmark
  environment: prod
  debug: false
  log_level: WARNING

data:
  root: datasets
  allow_holdout_eval: true
  sample_limit: null

evaluation:
  output_dir: evaluation/reports/prod
  fail_on_threshold: true

