app:
  name: pranik-benchmark
  environment: dev
  debug: true
  log_level: INFO

data:
  root: datasets
  allow_holdout_eval: false
  sample_limit: 100

evaluation:
  output_dir: evaluation/reports/dev
  fail_on_threshold: false

